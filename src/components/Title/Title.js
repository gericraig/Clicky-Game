import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>Memory Game!</h1>
        <p>Click on an image to earn points, but don't click on any more than once, see how good your memory is!</p>
    </div>
);
export default Title;